# Gato Neura

<p align="center">
  <a href="README.md"><img src="https://img.shields.io/badge/lang-en-blue" alt="English"></a>
  <a href="README.es.md"><img src="https://img.shields.io/badge/lang-es-green" alt="Español"></a>
  <img src="https://img.shields.io/badge/lang-zh-red" alt="中文"></a>
  <a href="README.ja.md"><img src="https://img.shields.io/badge/lang-ja-orange" alt="日本語"></a>
</p>

> **一个AlphaZero风格的神经网络，完全在浏览器中学习井字棋。**

无后端。无云端。无API密钥。只有你的浏览器、TensorFlow.js，以及一个通过自我对弈和蒙特卡洛树搜索自学井字棋的小型神经网络。

**在线演示：** [https://feojeda.github.io/gato-neura/](https://feojeda.github.io/gato-neura/)

**非技术解释：** [docs/EXPLAINED.zh.md](docs/EXPLAINED.zh.md)

---

## 目录

- [这是什么？](#这是什么)
- [快速开始](#快速开始)
- [如何使用](#如何使用)
- [理解指标](#理解指标)
- [背后的数学](#背后的数学)
- [架构](#架构)
- [训练阶段](#训练阶段)
- [多语言支持](#多语言支持)
- [贡献](#贡献)
- [许可证](#许可证)

---

## 这是什么？

Gato Neura是DeepMind的AlphaZero核心思想的**自包含教育实现**，规模缩小到井字棋，以便完全在Web浏览器中运行。

它展示了：
- **双头神经网络**（策略 + 价值）
- **自我对弈强化学习**
- **蒙特卡洛树搜索（MCTS）** 用于走法选择
- **课程学习**（随机对手 → 自身快照）
- **实时可视化**权重、架构和训练指标

全部代码仅约2,000行vanilla JavaScript，**零构建步骤**。打开DevTools即可阅读实际运行的代码。

---

## 快速开始

```bash
# 克隆
git clone https://github.com/feojeda/gato-neura.git
cd gato-neura

# 本地启动（任何静态服务器均可）
python3 -m http.server 8080 --directory public
# 或
npx serve public

# 打开 http://localhost:8080
```

无需 `npm install`。无需打包器。无需编译。

---

## 如何使用

界面分为三个面板：

### 1. 模型面板（左侧）
显示神经网络架构的实时SVG图。点击任意节点查看其传入权重的热力图。

### 2. 棋盘面板（中间）
游戏本身。选择谁先手，设置游戏温度，与训练好的网络对弈。

- **游戏温度**：`0.0` = 总是最佳走法（贪心）。`>1.0` = 有创意，有时随机。
- **策略提示**：在网络的回合，每个空格会显示百分比，表示网络的置信度。

### 3. 控制面板（右侧）

#### 模型架构
训练前添加/移除隐藏层。默认：`[64, 32]`。

> **提示：** 对于井字棋，2-3层32-64个神经元已经足够。更多层不会帮助，反而可能减缓收敛。

#### 训练设置

| 设置 | 默认值 | 作用 |
|------|--------|------|
| **对局数** | 500 | 生成的自我对弈总局数 |
| **学习率** | 0.0003 | 梯度下降的步长 |
| **批量大小** | 64 | 每次更新的训练样本数 |
| **MCTS模拟次数** | 50 | 每步模拟次数（0 = 禁用） |
| **增量训练** | 关 | 添加到现有模型或从头开始 |

#### 训练按钮
点击**训练**，实时观察指标更新。训练期间游戏界面会暂停。

---

## 理解指标

### 胜率
网络在当前训练会话中获胜的对局百分比。

| 徽章 | 含义 |
|------|------|
| 🟢 优秀 (>80%) | 完全压制当前对手 |
| 🔵 良好 (60-80%) | 赢得大部分，仍在学习 |
| 🟡 一般 (40-60%) | 尚未压制 |
| 🔴 较差 (<40%) | 输多赢少 |

> 训练胜率针对**混合对手**（随机 + 旧快照）。真正的测试是**对战随机质量**。

### 策略损失
网络预测*走哪一步*的能力。

越低越好。9个格子时，纯随机猜测的基准损失约为2.197。损失2.1意味着只比掷骰子好约5%。

| 徽章 | 阈值 |
|------|------|
| 🟢 优秀 | < 1.0 |
| 🔵 良好 | 1.0 - 1.5 |
| 🟡 一般 | 1.5 - 2.0 |
| 🔴 较差 | > 2.0 |

### 价值损失
网络评估局面是赢(+1)、输(-1)还是平局(0)的能力。

这是均方误差(MSE)。值越低 = 评估越好。

| 徽章 | 阈值 |
|------|------|
| 🟢 优秀 | < 0.3 |
| 🔵 良好 | 0.3 - 0.6 |
| 🟡 一般 | 0.6 - 1.0 |
| 🔴 较差 | > 1.0 |

> 低价值损失不等于下得好。网络可能评估很好，但不知道*该走什么*。

### 对战随机质量
训练结束后，网络进行**100局贪心对局**对抗纯随机对手。这是 ground-truth 指标。

| 结果 | 解读 |
|------|------|
| >90% | 接近完美。击败任何 casual 玩家。 |
| 70-90% | 很强，偶尔犯错。 |
| 50-70% | 懂基础，漏掉战术。 |
| <50% | 比随机还差。需要更多训练或更大的网络。 |

### 损失图表
实时绘制最近100个训练批次的策略损失（蓝色）和价值损失（红色）。

---

## 背后的数学

### 神经网络架构

网络是一个具有两个输出头的**多层感知机**：

```
输入(9) -> 全连接(64, ReLU) -> 全连接(32, ReLU) -> [策略(9, softmax), 价值(1, tanh)]
```

**输入编码：**
- +1：网络的棋子
- -1：对手的棋子
- 0：空格

**视角不变性：** 网络总是将自己视为+1。如果作为O下棋，输入前会翻转棋盘。

### 策略头

使用softmax输出9个格子的概率分布。非法走法（已占格子）的概率被掩码为0，然后重新归一化。

### 策略损失：交叉熵

网络被训练以匹配目标策略（来自MCTS访问计数）：

```
L_P = -mean( sum( pi * log(p_pred) ) )
```

为数值稳定性进行裁剪（epsilon = 1e-7）。

### 价值损失：均方误差

```
L_V = mean( (z - v_pred)^2 )
```

其中 z ∈ {-1, 0, +1} 是游戏结果。

### 总损失

```
L = L_P + L_V
```

### 温度采样

训练期间，以温度T采样走法来平衡探索与利用：

```
p_i = p_i^(1/T) / sum( p_j^(1/T) )
```

- T = 1.0：从原始策略采样（高探索）
- T -> 0：总是选择最高概率（纯利用）

### 蒙特卡洛树搜索（MCTS）

约50局后激活MCTS。每步运行N次模拟：

1. **选择**：使用UCB1分数遍历游戏树。
2. **扩展**：到达未访问节点时，使用网络的策略输出扩展。
3. **评估**：对叶节点使用网络的价值估计。
4. **回传**：将结果向上传播，每层翻转符号（零和博弈）。
5. **走法选择**：按访问计数比例下棋。

每次模拟需要网络一次前向传播，所以50次模拟 ≈ 比贪心慢50倍。

---

## 架构

```
public/
├── index.html              # 主页面
├── css/styles.css          # 响应式布局
└── js/
    ├── app.js              # 编排器，游戏循环
    ├── game.js             # 纯游戏逻辑（不可变）
    ├── model.js            # TF.js 模型创建/推理
    ├── trainer.js          # 自我对弈 + MCTS + 训练循环
    ├── ui.js               # DOM绑定，指标，i18n
    ├── visualizer.js       # SVG模型渲染
    ├── i18n.js             # 轻量级i18n引擎
    └── translations/       # en, es, zh, ja
        ├── en.js
        ├── es.js
        ├── zh.js
        └── ja.js
```

**设计理念：** 零构建步骤。DevTools中看到的代码就是实际运行的代码。无webpack，无npm，无转译。

---

## 训练阶段

训练课程分为三个阶段：

| 阶段 | 对局 | 对手 | MCTS | 目的 |
|------|------|------|------|------|
| 1 | 0-50 | 随机 | 否 | 学习基本获胜模式 |
| 2 | 50-100 | 随机 + 快照 | 是 | 通过树搜索学习战术 |
| 3 | 100+ | 快照（自身） | 是 | 针对过去版本优化 |

每100局对模型进行**快照**。这个旧版本成为对手，迫使当前模型对抗更强的敌人。

---

## 多语言支持

Gato Neura 开箱支持4种语言：

- 🇺🇸 **English**（默认）
- 🇪🇸 **Español**
- 🇨🇳 **中文（简体中文）**
- 🇯🇵 **日本語**

通过标题栏下拉菜单切换语言。偏好设置保存到 `localStorage`。

---

## 贡献

这个项目故意保持小巧易读。欢迎贡献！

### 我们期待的帮助

- [ ] **Web Workers**：将训练移至后台线程，避免UI卡顿
- [ ] **保存/加载模型**：导出训练好的权重（JSON或localStorage）
- [ ] **更好的可视化**：每位置策略热力图、激活图
- [ ] **更多游戏**：四连棋、跳棋或Hex，使用相同引擎
- [ ] **手动MCTS检查**：点击"思考"查看搜索树
- [ ] **ELO估算**：追踪随时间变化的估计等级分
- [ ] **更多语言**：你会说的任何语言
- [ ] **测试**：更多Playwright边界情况覆盖

### 如何贡献

1. Fork 仓库
2. 创建分支：`git checkout -b feature/my-idea`
3. 进行修改
4. 运行测试：`node test-integration.mjs`
5. 向 `develop` 提交 Pull Request

无需CLA，无需繁文缛节。只要对项目有帮助，我们就会合并。

**有问题或想法？** 提交 [Issue](https://github.com/feojeda/gato-neura/issues) 或开启 [Discussion](https://github.com/feojeda/gato-neura/discussions)。

---

## 许可证

MIT © [Francisco Ojeda](https://github.com/feojeda)
